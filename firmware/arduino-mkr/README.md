# firmware/arduino-mkr

PlatformIO project for Arduino MKR WiFi 1010. MQTT publisher via WiFiNINA + PubSubClient (adaptable from the prior work's `main.cpp` — but do NOT copy its committed secrets).

Must emit the shared envelope/measurements payload and publish to `slope/{site_id}/{device_id}/data`.

Not yet implemented.
